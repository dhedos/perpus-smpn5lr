
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // 1. Profil Pengguna & Pengaturan Dasar
    match /users/{userId} {
      allow read: if true;
      allow write: if isSignedIn();
    }
    match /users {
      allow list: if true;
    }

    match /settings/{docId} {
      allow read: if true;
      allow write: if isSignedIn();
    }

    // 2. Izin Penuh untuk Petugas Terdaftar pada koleksi utama
    // Menggunakan recursive wildcard untuk menjamin akses list/get pada seluruh dokumen
    
    match /members/{allPaths=**} {
      allow read, write: if isSignedIn();
    }
    
    match /books/{allPaths=**} {
      allow read, write: if isSignedIn();
    }
    
    match /transactions/{allPaths=**} {
      allow read, write: if isSignedIn();
    }
    
    match /activity_logs/{allPaths=**} {
      allow read, write: if isSignedIn();
    }

    // 3. Fallback Global untuk Keamanan
    match /{path=**} {
      allow read, write: if isSignedIn();
    }
  }
}
