
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * PERPUSTAKAAN SEKOLAH - EXPLICIT SECURITY RULES
     * Memberikan izin eksplisit untuk menghindari error "Missing or insufficient permissions"
     */

    // 1. Pengaturan Umum: Bisa dibaca publik untuk identitas di halaman Login
    match /settings/general {
      allow get, list: if true;
      allow write: if request.auth != null;
    }

    // 2. Koleksi Anggota (Members) - Izin penuh untuk petugas yang login
    match /members/{docId} {
      allow read, write: if request.auth != null;
    }

    // 3. Koleksi Buku (Books)
    match /books/{docId} {
      allow read, write: if request.auth != null;
    }

    // 4. Koleksi Transaksi (Transactions)
    match /transactions/{docId} {
      allow read, write: if request.auth != null;
    }

    // 5. Koleksi Log Aktivitas (Activity Logs)
    match /activity_logs/{docId} {
      allow read, write: if request.auth != null;
    }

    // 6. Koleksi Pengguna (Users)
    match /users/{docId} {
      allow read, write: if request.auth != null;
    }

    // 7. Fallback Global
    match /{path=**} {
      allow read, write: if request.auth != null;
    }
  }
}
